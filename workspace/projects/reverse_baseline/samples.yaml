# Reverse baseline sample manifest (Phase 0 scaffold)
# Fill with real local sample paths before running scripts/reverse_baseline_run.sh.
#
# Example:
# samples:
#   - path: /bin/ls
#   - path: /path/to/sample.exe
#   - path: /path/to/sample.apk

samples:
  - path: /bin/ls
  - path: /usr/bin/ls
