id: rscan-basic-http
info:
  name: Basic HTTP Status Probe
  severity: info
  tags: [default, health]
http:
  - method: GET
    path:
      - /
      - /robots.txt
    matchers:
      - type: status
        status: [200, 204, 301, 302, 401, 403]
